---
virtualization: virtualbox
system_brews:
  - dockutil
  - packer
  - packer-completion
  - vagrant-completion
# system_casks:
  # - vagrant (2.2.15 installed separately due to bug in 2.2.16)
  # - virtualbox (installed separately due to kernel ext failure)
dockitems_to_add:
  - name: VirtualBox
    path: "/Applications/VirtualBox.app"
