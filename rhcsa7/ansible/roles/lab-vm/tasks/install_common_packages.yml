---
- name: Ensure packages common to lab VMs are installed
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - nss-pam-ldapd
    - ipa-client
    - tcpdump
    - bind-utils
    - vim
    - screen
    - net-tools
    - nfs-utils
    - tmux
  become: yes
