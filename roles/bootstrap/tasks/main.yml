---
- name: Ensure public key is copied to authorized keys on host
  authorized_key:
    user: user
    state: present
    key: "{{ lookup('file', '{{ ansible_user_dir }}/.ssh/user.pub') }}"
- name: Ensure FQDN is set on host
  hostname:
    name: "{{ inventory_hostname }}"
  become: yes
