---
- name: Setup the KVM demo environment
  hosts: localhost
  roles:
    - role: vmhost
    - role: setup-packer
    - role: setup-vagrant
    - role: setup-kvm
    - role: setup-configure-host
    - role: setup-virtual-networks
    - role: setup-vagrant-base-boxes
