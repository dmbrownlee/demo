---
- name: Setup the VirtualBox environment
  hosts: localhost
  roles:
    - role: setup-directories
    - role: setup-packer
    - role: setup-vagrant
    - role: setup-kvm
    - role: setup-configure-host
#    - role: setup-virtual-networks
    - role: setup-vagrant-base-boxes
