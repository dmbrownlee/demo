---
- name: Ensure vagrant user is hidden from gdm login screen
  copy:
    src: vagrant
    dest: /var/lib/AccountsService/users
    mode: 0644
  become: yes
- include: install_common_packages.yml
- include: add_lab_user.yml
