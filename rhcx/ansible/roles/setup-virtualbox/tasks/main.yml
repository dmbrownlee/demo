---
# Installing KVM 
- name: install kvm 
  yum:
    name: "{{ item }}"
    state: present
  loop:
     - "@virtualization-hypervisor"
     - "@virtualization-client"
     - "@virtualization-platform"
     - "@virtualization-tools" 
  become: true
