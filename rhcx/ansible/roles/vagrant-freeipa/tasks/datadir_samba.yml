---
- name: Ensure export directories exists
  file:
    path: /srv/samba
    owner: root
    group: root
    mode: '0755'
    state: directory
  become: yes
- name: Ensure Samba is configured
  copy:
    dest: /etc/samba/smb.conf
    src: smb.conf
  become: yes
